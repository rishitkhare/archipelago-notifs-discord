steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '--tag', 'gcr.io/$PROJECT_ID/my-image', '.']
  env:
  - 'DOCKER_BUILDKIT=1'
images:
- 'gcr.io/$PROJECT_ID'

options:
  logging: CLOUD_LOGGING_ONLY